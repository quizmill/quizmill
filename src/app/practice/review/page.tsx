import { PackReviewRunner } from '@/pack/ReviewRunner';

/** Mistakes-review flow — re-asks the exact questions answered wrong. */
export default function ReviewPage() {
  return <PackReviewRunner />;
}
