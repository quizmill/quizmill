import { PacksPage } from '@/components/PacksPage';

/**
 * The pack shelf — insert, eject, and swap learning packs. One pack is
 * active at a time; progress is stored per pack, so swapping is free.
 */
export default function PacksRoute() {
  return <PacksPage />;
}
